/**
 * The board — masonry grid of saved items, "the counter" + filters.
 * Design contract: Figma "01 · the board".
 * TODO(next): query items for the signed-in user, render card variants
 * per item.kind (each kind has its own card anatomy — see mockup).
 */
export default function BoardPage() {
  return <main className="p-8 text-sm text-zinc-500">the board — coming out of the oven</main>;
}
