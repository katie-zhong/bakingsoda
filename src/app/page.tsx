/**
 * Marketing landing — placeholder until it gets designed properly.
 * (The real landing is a design task against the brand system, not a
 * scaffold task. Resist the urge to ship a template hero.)
 */
export default function Home() {
  return (
    <main className="flex min-h-dvh items-center justify-center">
      <p className="text-sm text-zinc-500">
        bakingsoda<span className="text-orange-600"> ●</span> — fermenting
      </p>
    </main>
  );
}
